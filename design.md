# Dashboard design

2026-09-21 기준. 헤더 이미지 제거 후 적용된 스타일을 기록합니다.

## 1. 디자인 방향

무채색 배경과 얇은 테두리를 중심으로 구성한 데이터 대시보드입니다. 글자 크기, 명도, 간격으로 정보 순서를 구분합니다. 분류는 이모지와 텍스트를 함께 표시합니다.

- 기본 테마는 다크입니다. 라이트와 시스템 설정도 지원합니다.
- 헤더는 단색입니다. 배경 이미지, 이미지 오버레이, 제목 그림자를 사용하지 않습니다.
- 검색 조건을 데이터 위에 둡니다.
- 패널은 1px 테두리로 구분합니다. 상시 그림자는 사용하지 않습니다.
- 선택한 필터는 배경과 글자 명도를 반전합니다.
- 장식용 색을 늘리지 않습니다. 형태별 분석에만 분류 색을 씁니다.

## 2. 구현 파일

| 파일 | 역할 |
| --- | --- |
| site/design-tokens.css | 테마 색상, 글꼴, 공통 크기 |
| site/style.css | 레이아웃, 컴포넌트, 반응형 규칙 |
| site/index.html | 화면 구조와 접근성 이름 |
| site/app.js | 검색, 필터, 정렬, 차트, 테마 동작 |

CSS는 design-tokens.css 다음에 style.css를 불러옵니다. 실제 적용값은 style.css의 컴포넌트 규칙이 우선합니다. 예를 들어 --font-section은 24px이지만 현재 h2는 21px입니다.

## 3. 색상

| 역할 / 변수 | 다크 | 라이트 |
| --- | --- | --- |
| 페이지 / --page | #0a0a0a | #fbfbfb |
| 패널 / --surface | #101010 | #f8f8f8 |
| 팝오버 / --surface-raised | #171717 | #ffffff |
| 주요 글자 / --ink | #fafafa | #0a0a0a |
| 일반 글자 / --ink-2 | #d4d4d4 | #404040 |
| 보조 글자 / --muted | #a3a3a3 | #737373 |
| 테두리 / --border | #262626 | #e6e6e6 |
| 축 / --axis | #404040 | #b8b8b8 |
| 차트 격자 / --grid | #252525 | #e8e8e8 |
| 호버 / --wash | #191919 | #f1f1f1 |
| 선택 배경 / --accent | #fafafa | #171717 |
| 선택 글자 / --on-accent | #0a0a0a | #fafafa |
| 초점 / --focus | #d4d4d4 | #525252 |
| 성공 / --success | #34d399 | #047857 |
| 오류 / --danger | #f87171 | #b91c1c |

성공과 오류 색상은 토큰으로 준비되어 있습니다. 모든 상태가 현재 화면에 표시되는 것은 아닙니다.

## 4. 글꼴과 타이포그래피

본문 글꼴 순서:

```css
font-family: "Noto Sans", "Noto Sans KR", system-ui,
  -apple-system, "Segoe UI", "Malgun Gothic", sans-serif;
```

글꼴 파일을 외부에서 불러오지 않습니다. 설치된 글꼴과 운영체제에 따라 글자 폭이 달라집니다.

| 요소 | 데스크톱 | 모바일 767px 이하 |
| --- | --- | --- |
| 메인 제목 | 40px / 1.2 / 700 | 30px / 1.2 / 700 |
| 제목 자간 | -1px | -1px |
| 헤더 보조 문구 | 16px / 1.6 | 13px / 1.6 |
| 구역 제목 h2 | 21px / 1.4 / 600 | 19px / 1.4 / 600 |
| 분석 패널 제목 | 18px | 18px |
| 본문 | 14px / 1.6 | 14px / 1.6 |
| 목록 항목 제목 | 14px / 1.5 / 600 | 16px / 1.5 / 600 |
| 검색 입력 | 13px | 16px |
| 보조 정보 | 주로 11~12px | 주로 11~12px |

숫자와 날짜에는 font-variant-numeric: tabular-nums를 적용합니다. 한국어 본문은 word-break: keep-all을 사용하고, 긴 목록 제목에는 overflow-wrap: anywhere를 적용합니다.

## 5. 레이아웃과 간격

- 내용 최대 폭은 1200px입니다. 데스크톱 컨테이너는 좌우 24px 패딩을 포함해 최대 1248px입니다.
- 상단 메뉴는 고정 위치이며 높이는 64px입니다.
- 단색 제목 구역은 최소 높이 286px, 패딩 110px 24px 60px입니다.
- 제목 내부 최대 폭은 820px이며 가운데 정렬합니다.
- 요약 수치는 가로로 배치하고 1px 세로 구분선을 둡니다.
- 본문 구역 간격은 28px, 분석 패널 사이 간격은 16px입니다.
- 패널 내부 패딩은 24px, 모서리는 8px입니다.
- 입력창과 버튼 모서리는 6px입니다.
- 공통 간격 토큰은 4, 8, 12, 16, 24, 32, 48px입니다. 컴포넌트에는 7, 10, 20, 28px 같은 개별 간격도 존재합니다.

## 6. 컴포넌트

### 상단 메뉴

브랜드, 구역 이동 링크, 검색 버튼, 테마 버튼을 배치합니다. 배경은 --page, 글자는 --ink입니다. 아래쪽에 --border 색상의 1px 선을 표시합니다. 현재 구역 링크에는 2px 밑줄을 표시합니다.

### 검색과 필터

검색 구역은 카드로 감싸지 않습니다. 아래쪽 구분선으로 결과와 나눕니다.

데스크톱에서는 검색창, 게임, 연도, 지역, 초기화 버튼을 한 행에 놓습니다. 선택 입력과 초기화 버튼 높이는 36px입니다. 검색창도 테두리를 포함해 36px입니다.

형태 필터는 줄바꿈 가능한 버튼 묶음입니다. 기본 높이는 34px입니다. 선택 상태는 aria-pressed="true"와 반전 색상으로 표시합니다. 활성 조건이 없으면 초기화를 비활성화합니다.

### 타임라인

연도 축과 게임별 행을 사용합니다. 표식은 형태 이모지입니다. 겹치는 표식은 다음 줄에 배치하며 줄 높이는 22px입니다. 데이터 밀도에 따라 차트 높이가 늘어납니다.

차트 최소 폭은 1120px입니다. 좁은 화면에서는 차트 내부만 가로로 스크롤합니다. 표식을 누르면 대응하는 목록 항목으로 이동합니다.

### 분석 패널

형태별, 지역별, 연도별 분석을 나란히 배치합니다. 막대 높이는 5px입니다. 라벨, 막대, 숫자를 같은 열에 맞춥니다. 형태별 막대는 분류 색상, 지역별과 연도별 막대는 무채색을 사용합니다.

### 목록

데스크톱은 게임과 파트너, 형태, 지역, 날짜, 공지의 다섯 열로 구성합니다. 행 사이에는 1px 선을 둡니다. 호버 배경은 --wash입니다. 공지가 여러 개면 details와 summary로 접습니다.

검색 결과가 없으면 빈 결과 문구와 초기화 버튼을 표시합니다. 데이터 로드 실패 시 다시 시도 버튼을 표시합니다.

### 툴팁

최대 폭은 300px이며 화면 폭에서 최소 16px를 제외합니다. 패딩은 12px, 모서리는 6px입니다. 팝오버에만 0 8px 32px #0005 그림자를 사용합니다.

## 7. 이모지와 분류 색상

| 형태 | 이모지 | 토큰 | 다크 | 라이트 |
| --- | --- | --- | --- | --- |
| 인게임 | 🎮 | --form-1 | #60a5fa | #2563eb |
| 식음료 | 🍔 | --form-2 | #fbbf24 | #b45309 |
| 카페 및 팝업 | ☕ | --form-3 | #34d399 | #047857 |
| 굿즈 및 하드웨어 | 🎧 | --form-4 | #c4b5fd | #7c3aed |
| 브랜드 | 🏷️ | --form-5 | #f472b6 | #be185d |
| 음악 | 🎵 | --form-6 | #67e8f9 | #0e7490 |
| 기타 | 📌 | --form-7 | #a3a3a3 | #737373 |

필터, 범례, 타임라인, 분석, 목록에서 같은 대응표를 씁니다. 일반 이모지 크기는 16px이며 자리는 20×20px입니다. 분석 라벨에서는 15px을 사용합니다. 이모지 옆에 텍스트가 있으면 중복 낭독을 막도록 이모지에 aria-hidden을 지정합니다.

## 8. 반응형 규칙

| 화면 폭 | 변경 |
| --- | --- |
| 1100px 이하 | 분석 2열, 마지막 패널 전체 폭, 연도별 막대 2열, 메뉴 간격 축소 |
| 850px 이하 | 좌우 패딩 20px, 검색창 전체 행, 나머지 검색 조건 3열, 패널 패딩 20px |
| 767px 이하 | 좌우 패딩 16px, 분석 1열, 검색 조건 2열, 패널 패딩 18px, 구역 간격 20px |
| 360px 이하 | 제목 27px, 브랜드 14px, 일부 헤더 줄바꿈 |

767px 이하에서는 구역 메뉴를 화면 하단에 고정합니다. 높이는 60px에 safe-area-inset-bottom을 더합니다. 본문 아래에도 88px와 안전 여백을 확보합니다.

모바일 제목 구역은 최소 높이 248px, 패딩 102px 20px 52px입니다. 요약 수치는 2×2로 배치합니다. 목록 항목은 제목을 먼저 놓고 형태, 날짜, 지역, 공지를 아래에 배치합니다.

모바일 필터, 정렬, 초기화 버튼은 최소 높이 44px입니다. 현재 상단 검색과 테마 버튼은 최소 36px입니다. 전체 조작부가 44px인 것은 아닙니다.

## 9. 상태와 접근성

- 키보드 초점은 2px 윤곽선과 4px 바깥 간격으로 표시합니다.
- 검색창은 focus-within에 2px 윤곽선과 2px 간격을 적용합니다.
- Ctrl K 또는 Command K로 검색창에 초점을 옮깁니다.
- 고정 메뉴를 고려해 구역 이동의 상단 여백은 88px, 모바일은 80px입니다.
- 타임라인 표식은 키보드로 실행하고 대응 목록으로 이동합니다.
- 페이지 앞에 목록으로 건너뛰기 링크를 둡니다.
- 전환 시간은 150ms입니다. prefers-reduced-motion에서는 부드러운 스크롤과 CSS 전환을 끕니다.
- 색이나 이모지만으로 분류를 전달하지 않습니다. 텍스트 이름을 함께 제공합니다.

## 10. 다른 사이트에 적용

먼저 design-tokens.css를 불러온 다음 해당 사이트의 컴포넌트 CSS를 연결합니다.

```html
<html lang="ko" data-theme="dark">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="design-tokens.css">
  <link rel="stylesheet" href="style.css">
</head>
```

```css
.panel {
  padding: 24px;
  background: var(--surface);
  color: var(--ink);
  border: 1px solid var(--border);
  border-radius: var(--radius-panel);
}

.filter[aria-pressed="true"] {
  background: var(--accent);
  color: var(--on-accent);
  border-color: var(--accent);
}
```

html의 data-theme를 light 또는 dark로 지정합니다. 시스템 설정 선택은 JavaScript에서 실제 테마 값으로 변환합니다. 현재 선택 저장 키는 collab-atlas-theme입니다. 다른 서비스에서는 별도 키를 사용하세요.

당신의 사이트에 적용할 때 서비스 이름과 데이터 구조를 교체하세요. 단색 헤더, 무채색 선택 상태, 얇은 테두리, 분류 이모지 규칙은 유지하세요. 검색, 정렬, 빈 결과, 키보드 초점은 해당 데이터에 연결하세요.

390px, 800px, 1440px에서 긴 제목, 빈 결과, 두 테마, 마지막 목록 행을 확인하세요. 차트 외의 페이지 전체에 가로 스크롤이 생기지 않아야 합니다.

## 11. 참고와 적용 범위

디자인 참고: https://revenue.ennead.cc/ 및 https://revenue.ennead.cc/revenue

무채색 테마, 얇은 경계선, 작은 조작부, 데이터 행 구성을 참고했습니다. 이 문서는 참고 사이트의 공식 디자인 명세가 아닙니다.

현재 대시보드는 요청에 따라 헤더 이미지를 제거했습니다. 외부 이미지 주소와 배경 이미지 레이어가 없습니다. 내용 폭, 제목 크기, 분류 이모지와 모바일 메뉴는 이 대시보드에 맞춘 값입니다.
